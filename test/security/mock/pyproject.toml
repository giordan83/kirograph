[tool.poetry]
name = "mock-lib"
version = "0.1.0"
description = "Mock Python library"

[tool.poetry.dependencies]
python = "^3.11"
fastapi = "^0.104.1"
httpx = "^0.25.2"

[tool.poetry.group.dev.dependencies]
pytest = "^7.4.3"
black = "^23.11.0"
